import 'package:flutter/material.dart';

class TransactionResultPage extends StatelessWidget {
  const TransactionResultPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
