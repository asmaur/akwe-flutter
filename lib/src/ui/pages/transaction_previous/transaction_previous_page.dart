import 'package:flutter/material.dart';

class TransactionPreviousPage extends StatelessWidget {
  const TransactionPreviousPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold();
  }
}
