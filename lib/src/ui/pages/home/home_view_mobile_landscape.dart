import 'package:flutter/material.dart';

class HomeViewMobileLandscape extends StatelessWidget {
  const HomeViewMobileLandscape({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(child: Text("Mobile land"),),
    );
  }
}
