import 'package:flutter/material.dart';

class PlanningAllPage extends StatelessWidget {
  const PlanningAllPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
