import 'package:flutter/material.dart';

class AccountTileLarge extends StatelessWidget {
  const AccountTileLarge({super.key, required account});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
