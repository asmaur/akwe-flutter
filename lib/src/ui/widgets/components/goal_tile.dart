import 'package:flutter/material.dart';

class GoalTile extends StatelessWidget {
  const GoalTile({super.key, required goal});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
