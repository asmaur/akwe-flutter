import 'package:flutter/material.dart';

class RoutineTile extends StatelessWidget {
  const RoutineTile({super.key, required routine});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
