enum CategoryPopupMenuItem {edit, archived}
enum AccountPopupMenuItem {edit, archived}
enum FilterPopupMenuItem {all, previous}
enum AppPopupMenuItem {view, edit, archived, all, pPrevious, delete, restore }
enum AppPopupExportMenu {excel, csv, pdf}